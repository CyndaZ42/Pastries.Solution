@{
  Layout ="_Layout";
}

@model Pastries.Models.Treat

<h1>Treat Details</h1>

<h2>@Html.DisplayNameFor(model => model.TreatName): @Html.DisplayFor(model => model.TreatName)</h2>

@if(@Model.JoinEntities.Count == 0)
{
  <p>This treat is very bland.</p>
}
else
{
  <h4>Treats with this treat:</h4>
  <ul>
  @foreach(var join in Model.JoinEntities)
  {
    <li>This is a @join.Flavor.FlavorName treat!</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to treats", "Index")</p>
<p>@Html.ActionLink("Edit Treat", "Edit", new { id = Model.TreatId })</p>
<p>@Html.ActionLink("Delete Treat", "Delete", new { id = Model.TreatId })</p>